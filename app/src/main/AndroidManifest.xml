<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusLock2">

        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Add this line -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BlockerActivity" android:exported="false" />

        <activity android:name=".VerifyAccessActivity" android:exported="false"/>
        <activity android:name=".ParentDashboardActivity" android:exported="false"/>
        <activity android:name=".CompletionLogsActivity" android:exported="false"/>
        <activity android:name=".OptionsActivity" android:exported="false"/>
        <activity android:name=".TasksActivity" android:exported="false"/>
        <activity android:name=".SetTimeoutAppsActivity" android:exported="false"/>
        <activity android:name=".SetTasksActivity" android:exported="false"/>
        <!-- add other activities with exported true/false as needed -->

        <service
            android:name=".FocusLockAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".AppBlockerOverlayService"
            android:exported="false" />

    </application>

</manifest>
